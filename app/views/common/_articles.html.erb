<div class="panel panel-default">

  <div  class="list-group">
    <% @articles.each do |article| %>
        <%= link_to article.title, article_path(article.id), class: "list-group-item" %>
    <% end %>
  </div>

</div>